#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);  // Read N first

    int A[N];  // Now we can declare the array with size N
for (int i = 0; i <=N; i++)
{
    /* code */
    
}

  

    return 0;
}
